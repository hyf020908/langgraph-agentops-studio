# LangGraph for Multi-Agent Workflows

LangGraph offers durable state transitions, checkpointing, interrupt/resume control flow,
and deterministic routing. These capabilities are useful for workflows that require
auditable reviewer loops and human approval gates.
